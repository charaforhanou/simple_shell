#include "shell.h"

char *ptr:
int i,len=0;
if (str == NULL)
return (NULL);
while (*str != '\0')
{
    len++
    str++;
}
str = str - len;
ptr = malloc(sizeof(char)*(len+1));
if(ptr == NULL)
return ()